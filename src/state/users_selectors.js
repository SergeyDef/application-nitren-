export const profile = (state) => {
  return state.userProfile.profile;
}
export const names = (state) => {
  return state.userProfile.name;
}
export const avatars = (state) => {
  return state.userProfile.avatar;
}

export const userData = (state) => {
  return state.userProfile.userData;
}