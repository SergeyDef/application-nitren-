import React from 'react';
import s from './WindowInfo.module.css'

const WindowInfo = () =>{
  
  return (
      <div className={s.WindowInfo}>
        
      </div>
    );
}

export default WindowInfo;
